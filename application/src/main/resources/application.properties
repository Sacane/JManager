server.port=8080

# Client address behind the reverse proxy. nginx appends the address it saw to X-Forwarded-For; with
# `native`, Tomcat reads that header from the right and skips trusted proxies (private ranges by default,
# which covers the Docker network), so an address a client puts on the left is ignored. Do not switch to
# `framework`: it reads from the left and would honour a forged value. Sign-in and password reset rate
# limits are keyed on this address.
server.forward-headers-strategy=native
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.PostgreSQLDialect
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.generate-ddl=false
spring.jpa.properties.hibernate.globally_quoted_identifiers=true
spring.jpa.open-in-view=false

spring.jpa.show-sql=false

spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.servlet.UserDetailsServiceAutoConfiguration

spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration

spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB

server.tomcat.max-http-post-size=2097152

# Application URL (used in emails)
app.url=${APP_URL:http://localhost:3000}

# Actuator
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=never
management.info.build.enabled=true

# Data retention (GDPR Art. 5(1)(e))
jmanager.retention.unconsented-account-days=30
jmanager.retention.cron=0 0 2 * * *

# Mail (SMTP)
spring.mail.host=${SMTP_HOST:localhost}
spring.mail.port=${SMTP_PORT:587}
spring.mail.username=${SMTP_USERNAME:resend}
spring.mail.password=${SMTP_PASSWORD:}
spring.mail.from=${SMTP_FROM:noreply@jmanager.sacane.fr}
spring.mail.properties.mail.smtp.auth=${SMTP_AUTH:true}
spring.mail.properties.mail.smtp.starttls.enable=${SMTP_STARTTLS:true}